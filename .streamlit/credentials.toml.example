# Copie para credentials.toml (arquivo local, não versionado):
#   cp .streamlit/credentials.toml.example .streamlit/credentials.toml
#
# Marca o onboarding como concluído sem coletar e-mail (valor vazio = pular).
# Se o Streamlit ainda perguntar, rode a partir da raiz do repositório.

[general]
email = ""
